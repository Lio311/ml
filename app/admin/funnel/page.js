import FunnelClient from './FunnelClient';

export const metadata = {
  title: "משפך מכירות Admin",
  robots: "noindex, nofollow",
};

export default function FunnelPage() {
  return <FunnelClient />;
}
