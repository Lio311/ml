# מיפוי בדיקות QA למערכת הלוגואים הדינמית (Dynamic Logos)

מסמך זה מרכז את כל הלוגואים והאייקונים שהפכו לדינמיים באתר, אילו עמודים/רכיבים משתמשים בהם, ואיפה יש לבדוק אותם ידנית.
המערכת החדשה מאפשרת לעדכן את כולם מפאנל הניהול בעמוד ה"לוגואים".

## 1. לוגו ראשי (Header)
**מזהה בקוד:** `logo_header`
**היכן משתמשים בקוד:**
- `app/app/components/Header.js` (תפריט עליון)
- `app/app/layout.js` (metadata & SEO images)
- `app/app/about/page.js` (SEO Schema)
- `app/app/about-2/page.js` (SEO Schema)
- `app/app/maintenance/MaintenanceClient.js` (עמוד "האתר בשיפוצים")

**כיצד לבדוק ידנית:**
1. היכנסו לאתר הראשי ובדקו שהלוגו בתפריט העליון מופיע כראוי.
2. העבירו את האתר למצב תחזוקה (מפאנל ניהול) ובדקו את הלוגו בעמוד "האתר בשיפוצים".
3. בדקו קוד מקור (View Source) בעמוד הבית וודאו שה-Schema מכיל את כתובת הלוגו הדינמית (`/api/assets/logo?type=logo_header`).

---

## 2. לוגו למיילים (Email)
**מזהה בקוד:** `logo_email`
**היכן משתמשים בקוד:**
- `app/app/lib/email.js` (תבניות המיילים הראשיות הנשלחות מהאתר, כגון אישור הזמנה, מעקב)
- `app/app/admin/mailing/MailingClient.js` (תצוגה מקדימה ושליחה של מיילים דרך מערכת הדיוור בפאנל ניהול)

**כיצד לבדוק ידנית:**
1. בצעו רכישת טסט או שלחו מייל טסט לעצמכם דרך מערכת הדיוור (`/admin/mailing`).
2. פתחו את המייל ובדקו שהלוגו (בכותרת או בגוף המייל) נטען כהלכה.

---

## 3. לוגו צ'אט (Chat)
**מזהה בקוד:** `logo_chat`
**היכן משתמשים בקוד:**
- `app/app/components/Chat/InboxClient.js` (תצוגת הפרופיל של הנציג/הבוט בתיבת הצ'אט)

**כיצד לבדוק ידנית:**
1. פתחו את חלון הצ'אט באתר (הבועה למטה).
2. בדקו שתמונת הפרופיל שמוצגת ליד ההודעות של האתר היא הלוגו/אייקון המוגדר.

---

## 4. לוגו גיבוי / אופליין (Fallback)
**מזהה בקוד:** `logo_fallback`
**היכן משתמשים בקוד:**
- `app/app/layout.js` (Open Graph images ברירת מחדל)
- `app/app/product/[slug]/page.js` (תמונת OG כשאין למוצר תמונה)
- `app/app/blog/page.js` & `app/app/blog/[slug]/page.js` (תמונות חלופיות למאמרים)
- `app/app/api/admin/push/send/route.js` (אייקון להודעות Push אם לא סופק אייקון ייעודי - בחלק מהמקרים)

**כיצד לבדוק ידנית:**
1. שלחו הודעת Push טסט דרך פאנל הניהול ללא תמונה ספציפית, ובדקו אם האייקון ברירת המחדל מופיע.
2. שתפו קישור לדף קטגוריה או מאמר ללא תמונה ייעודית ב-WhatsApp או Facebook וראו אם תמונת הגיבוי מופיעה כ-Preview.

---

## 5. אייקון לאפליקציה באייפון (Apple Touch Icon)
**מזהה בקוד:** `icon_apple`
**היכן משתמשים בקוד:**
- `app/app/layout.js` (תגית `<link rel="apple-touch-icon">`)

**כיצד לבדוק ידנית:**
1. פתחו את האתר מדפדפן Safari במכשיר אייפון.
2. לחצו על כפתור השיתוף בחרו ב-"Add to Home Screen" (הוסף למסך הבית).
3. ודאו שהאייקון שמופיע בחלון ההוספה תקין ומעודכן.

---

## 6. אייקון PWA (192x192)
**מזהה בקוד:** `icon_192`
**היכן משתמשים בקוד:**
- `app/app/manifest.js` (קובץ המניפסט של ה-PWA)
- `app/app/api/admin/push/send/route.js` (אייקון התראה ב-Push Notifications)

**כיצד לבדוק ידנית:**
1. פתחו את האתר ממכשיר אנדרואיד באמצעות Chrome ונסו להוריד את האפליקציה ("Add to Home Screen").
2. לחלופין, שלחו Push Notification ובדקו את האייקון הקטן שמופיע בהתראה (סמליל 192).

---

## 7. אייקון PWA (512x512)
**מזהה בקוד:** `icon_512`
**היכן משתמשים בקוד:**
- `app/app/layout.js` (Splash screens שונים באייפון / אנדרואיד בהתאם למידות)
- `app/app/manifest.js` (אייקון ה-PWA באיכות הגבוהה)

**כיצד לבדוק ידנית:**
1. כמו באייקון 192, בדקו שמירה למסך הבית.
2. פתחו את האפליקציה כ-PWA ממסך הבית (באייפון או אנדרואיד) וראו את מסך הטעינה (Splash Screen) שמופיע.

---

## 8. פאביקון (Favicon)
**מזהה בקוד:** `favicon`
**היכן משתמשים בקוד:**
- `app/app/layout.js` (תגית ה-`<link rel="icon">`)

**כיצד לבדוק ידנית:**
1. פתחו את האתר בדפדפן כרום/אדג'/פיירפוקס במחשב.
2. הסתכלו על הלשונית (Tab) של הדפדפן למעלה וודאו שהסמליל הקטן (הפאביקון) מוצג באופן תקין ומעודכן.
